name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write
  packages: write

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        run: pytest

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Docker image
        run: |
          docker build                                                                    \
            -t ghcr.io/${{ github.repository_owner }}/news-etl-api:${{ github.ref_name }} \
            -t ghcr.io/${{ github.repository_owner }}/news-etl-api:latest                 \
            .

      - name: Push Docker image
        run: |
          docker push ghcr.io/${{ github.repository_owner }}/news-etl-api:${{ github.ref_name }}
          docker push ghcr.io/${{ github.repository_owner }}/news-etl-api:latest

      - name: Create artifact
        run: |
          zip -r news-etl.zip .  \
            -x ".git/*"          \
            -x ".env"            \
            -x "__pycache__/*"   \
            -x "*/__pycache__/*" \
            -x ".pytest_cache/*"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: news-etl.zip
